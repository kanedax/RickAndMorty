import React from 'react';

// just a simple custom loading

const Waiting = () => {
    return (
        <div style={{width: '100%'}} className='mt-5'>
            <h4 className='text-white text-center ram-family'>Loading...</h4>
        </div>
    );
}

export default Waiting;
